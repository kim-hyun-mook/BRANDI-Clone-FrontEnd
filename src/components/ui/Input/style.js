import styled from "styled-components";

export const Input = styled.input`
	width: calc(100% - 40px);
  height: 38px;
  margin-top: -6px;
  vertical-align: top;
  opacity: 1;
	appearance: none;
	border: 0;
`